#pragma once
#include "polis_list.hpp"

// Sorts the PolisList in-place by umur (ascending)
void sortPolisListByUmur(PolisList& list);
// Sorts the PolisList in-place by risiko (ascending)
void sortPolisListByRisiko(PolisList& list);
